@model SO.SilList.Manager.Models.ViewModels.ListingTypeVm
@{
    ViewBag.Title = "Listing Type";
    Layout = "~/Views/Shared/_InnerLayout.cshtml";
}

<h2>@ViewBag.Title</h2>

@*
@Html.Action("List")
    *@

<form class="form-inline" method="get" action="@Url.Action("Index","ListingType")">

 
@Html.Action("Filter",@Model)

    <div class="pull-right">
        @((Model.paging.pageNumber-1) * Model.paging.rowCount + Model.result.Count) of @Model.paging.totalCount
    </div>
@Html.Partial("_List", @Model.result)


@Html.Action("Pagination", @Model.paging)


 </form>