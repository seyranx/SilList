@model IEnumerable<SO.SilList.Manager.Models.ValueObjects.EnvironmentTypeVo>

<p>
   <a href="@Url.Action("Create")" class="btn btn-info"> Create New </a>

</p>

<table class="table table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.url)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.created)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.url)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.created)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isActive)
        </td>
        <td>
            <a href="@Url.Action("Edit",new { id=item.environmentTypeId })" class="btn">Edit</a>
            <br>
            <a href="@Url.Action("Details",new { id=item.environmentTypeId })" class="btn">Details</a>
            <br>
            <a href="@Url.Action("Delete",new { id=item.environmentTypeId })" class="btn">Delete</a>
            @*@Html.ActionLink("Delete", "Delete", new { id=item.environmentTypeId })*@
        </td>
    </tr>
}

</table>
