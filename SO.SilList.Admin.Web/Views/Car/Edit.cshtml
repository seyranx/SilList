@model SO.SilList.Manager.Models.ValueObjects.CarVo

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_InnerLayout.cshtml";
}



<h2>@ViewBag.Title</h2>


@* @using (Html.BeginForm()) *@
@using (Html.BeginForm("Edit", "Car", FormMethod.Post, new { @encType = "multipart/form-data", id="myForm" }))
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend></legend>

        @Html.HiddenFor(model => model.carId)

        <div class="editor-label">
            @Html.LabelFor(model => model.modelTypeId)
        </div>
        <div class="editor-field">
            @Html.Action("DropDownList", "ModelType", new { id = Model.modelTypeId })
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.year)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.year)
            @Html.ValidationMessageFor(model => model.year)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.millage)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.millage)
            @Html.ValidationMessageFor(model => model.millage)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.carBodyTypeId)
        </div>
        <div class="editor-field">
            @Html.Action("DropDownList", "CarBodyType", new { id = Model.carBodyTypeId })
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.siteId)
        </div>
        <div class="editor-field">
            @Html.Action("DropDownList", "Site", new { id = Model.siteId })
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.transmissionTypeId)
        </div>
        <div class="editor-field">
            @Html.Action("DropDownList", "TransmissionType", new { id = Model.transmissionTypeId })
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.startDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.startDate)
            @Html.ValidationMessageFor(model => model.startDate)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.endDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.endDate)
            @Html.ValidationMessageFor(model => model.endDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.isApproved)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.isApproved)
            @Html.ValidationMessageFor(model => model.isApproved)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.isActive)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.isActive)
            @Html.ValidationMessageFor(model => model.isActive)
        </div>


        @* car image stuff below *@
        @{
    if (ViewBag.carImages != null)
    {
                @Html.Hidden("ImagesToRemove", null, new { id = "imagesToRemove" })
                
        int index = 0;
        foreach (SO.SilList.Manager.Models.ValueObjects.ImageVo img in ViewBag.carImages)
        {
                    <img src="@img.url" id="img_@index" />
        
            @:Uncheck to remove: 
            <input type="checkbox" id="removeImg_@index" checked="yes"/>

            
                    @* @Html.CheckBox("") *@

        }
                <br />
    }
        }
        @Html.Partial("../Image/_ImagePickerControls")  @* direct call *@
        <br />
        @* car images stuff above *@

        <p>
            <input type="submit" value="Save" />
            <a href="@Url.Action("Index")" class="btn btn-info">Back to List</a>
        </p>
        <hr />
                @*       <p>
                    @Html.Action("ImageListWithUploadForEdit", "Image" , new { id = Model.carId })
                </p>*@
    </fieldset>
}

<script type="text/javascript">
    $('#myForm').submit(function() {
        var numOfCheckboxes = @ViewBag.carImages.Count;
        alert(" 1 " + numOfCheckboxes.toString() );
        var myJSVariable = <%Viewbag.carImages.Count%>;
        alert(" 1 " + myJSVariable.toString() );
       // var yourVariable = @@Html.Raw(Json.Encode(ViewBag.yourVariable))
        var yourVariable= @Html.Raw(ViewBag.carImages.Count);

        alert(" 1 " + yourVariable.toString() );

</script>